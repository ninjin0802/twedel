# セキュリティポリシー

## 対応バージョン

最新版のみをサポートします。セキュリティ修正を受け取るため、GitHub Releasesまたはアプリ内更新から常に最新版へ更新してください。

## 脆弱性の報告

認証情報の漏えい、任意コード実行、ローカルAPIへの外部アクセスなど、セキュリティ上の問題を発見した場合は公開Issueへ詳細を書かず、GitHubの[非公開脆弱性報告](https://github.com/ninjin0802/twedel/security/advisories/new)を使用してください。

報告には、影響するバージョン、再現手順、想定される影響を含めてください。XのCookie、`auth_token`、`ct0`、個人情報、実際のポスト本文は添付しないでください。

## セキュリティ設計

- ローカルAPIは`127.0.0.1`だけで待ち受け、LANへ公開できないよう固定しています。
- APIはloopback以外のHostと外部サイトのOriginを拒否します。
- Electronはsandboxとcontext isolationを有効化し、rendererのNode.js統合を無効化しています。
- Electronから任意の外部ページを開く動作と、外部URLへの画面遷移を拒否します。
- Content Security Policy、クリックジャッキング防止、MIME sniffing防止などのHTTPヘッダーを送信します。
- Xの認証情報はPC内のアプリデータへ保存し、APIレスポンスや通常ログへ返しません。
- セッションファイルはOSが対応する場合、所有者だけが読み書きできる権限で作成します。
- 削除したポストの本文と削除結果は履歴ログへ保存しません。
- 自動更新パッケージはGitHub Releasesから`electron-updater`経由で取得します。

## 利用者の注意事項

- インストーラーは必ずこのリポジトリのGitHub Releasesから入手してください。
- PCを他人と共有する場合、Windowsアカウントを分けてください。
- Cookieや認証情報をIssue、スクリーンショット、チャットへ掲載しないでください。
- Xからログアウトした場合や不審な動作がある場合は、twedelの接続情報を削除してXのセッションも確認してください。
- 削除操作は元に戻せません。セキュリティ機構は誤った対象選択を復元するものではありません。

## 監査

リリース時に、全依存関係の`npm audit`、型チェック、関連テスト、Windowsビルドを実施します。Electronは開発依存として管理されていますが配布アプリへ組み込まれるため、監査対象から除外しません。監査結果はリリースノートへ記載します。
