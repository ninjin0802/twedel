## 更新内容

- 複数アカウント切り替え時、保存済みCookieが示す本人IDをXへ再確認するよう変更
- 保存されたアカウント表示とCookieの実アカウントが一致しない場合、誤操作防止のため切り替えを中止
- XがHTTP 200を返しても0件だった取得経路を固定せず、次の取得経路へ自動フォールバック
- 実データを返すタイムラインへ切り替わらず、取得中のまま0件になる問題を改善
- v0.8.0の保存済みアカウント個別削除・全アカウント設定リセット機能を同梱
