## 更新内容

- バージョン情報ページへ「開発を支援する」を追加
- 支援先としてOFUSEの`ninjin`ページを設定
- 支援ページはアプリ内へ読み込まず、OSの標準ブラウザで開く安全な構成
- 画面から任意のURLを渡せない固定リンク方式にし、外部リンクの悪用を防止
- 支援は完全に任意で、支援の有無による機能差がないことを明記
- READMEとプライバシーポリシーへOFUSE利用時の外部通信を追記

> SignPath Foundationへの申請準備段階です。このリリースのインストーラー自体はまだ未署名です。
